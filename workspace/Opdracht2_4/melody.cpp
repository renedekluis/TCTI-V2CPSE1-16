#include "melody.hpp" 

void melody::play(note_player & p ){
	p.play( note{ note::D5s, note::dQ } );
	p.play( note{ note::D5, note::dQ } );
	p.play( note{ note::D5s, note::dQ } );
	p.play( note{ note::F5s, note::dQ } );
	p.play( note{ note::G5s, note::dQ } );
	p.play( note{ note::F5s, note::dQ } );
	p.play( note{ note::F5, note::dQ } );
	p.play( note{ note::F5s, note::dQ } );
	p.play( note{ note::A5s, note::dQ } );
	p.play( note{ note::B5, note::dQ } );
	p.play( note{ note::A5s, note::dQ } );
	p.play( note{ note::A5, note::dQ } );
	p.play( note{ note::A5s, note::dQ } );
	p.play( note{ note::F6, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::D6, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::F6, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::D6, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::F6s, note::dQ } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::F6s, note::dH } );
	p.play( note{ note::C6s, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::F6, note::dQ } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::C6s, note::dH } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::C6s, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::F6, note::dQ } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::C6s, note::dH } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::C6s, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::F6, note::dQ } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::C6s, note::dH } );
	p.play( note{ note::C6, note::dH } );
	p.play( note{ note::A5s, note::dF } );
	p.play( note{ note::F5, note::dQ } );
	p.play( note{ note::D5s, note::dQ } );
	p.play( note{ note::D5, note::dQ } );
	p.play( note{ note::D5s, note::dQ } );
	p.play( note{ note::F5s, note::dF } );
	p.play( note{ note::G5s, note::dQ } );
	p.play( note{ note::F5s, note::dQ } );
	p.play( note{ note::F5, note::dQ } );
	p.play( note{ note::F5s, note::dQ } );
	p.play( note{ note::A5s, note::dF } );
	p.play( note{ note::B5, note::dQ } );
	p.play( note{ note::A5s, note::dQ } );
	p.play( note{ note::A5, note::dQ } );
	p.play( note{ note::A5s, note::dQ } );
	p.play( note{ note::F6, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::D6, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::F6, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::D6, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::F6s, note::dF } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::F6s, note::dH } );
	p.play( note{ note::C6s, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::F6, note::dQ } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::C6s, note::dH } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::C6s, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::F6, note::dQ } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::C6s, note::dH } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::C6s, note::dQ } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::F6, note::dQ } );
	p.play( note{ note::D6s, note::dH } );
	p.play( note{ note::C6s, note::dH } );
	p.play( note{ note::C6, note::dH } );
	p.play( note{ note::A5s, note::dF } );
	p.play( note{ note::A5s, note::dH } );
	p.play( note{ note::B5, note::dH } );
	p.play( note{ note::C6s, note::dH } );
	p.play( note{ note::C6s, note::dH } );
	p.play( note{ note::D6s, note::dQ } );
	p.play( note{ note::C6s, note::dQ } );
	p.play( note{ note::B5, note::dQ } );
}
