#include "melody.hpp" 

void melody::play(note_player & p ){
	p.play( note{ note::P, note::dQH } );
	p.play( note{ note::E5, note::dH } );
	p.play( note{ note::D5s, note::dH } );
	p.play( note{ note::E5, note::dH } );
	p.play( note{ note::D5s, note::dH } );
	p.play( note{ note::E5, note::dH } );
	p.play( note{ note::B5, note::dH } );
	p.play( note{ note::D5, note::dH } );
	p.play( note{ note::C5, note::dH } );
	p.play( note{ note::A5, note::dQH } );
	p.play( note{ note::P, note::dQH } );
	p.play( note{ note::C5, note::dH } );
	p.play( note{ note::E5, note::dH } );
	p.play( note{ note::A5, note::dH } );
	p.play( note{ note::B5, note::dQH } );
	p.play( note{ note::P, note::dQH } );
	p.play( note{ note::E5, note::dH } );
	p.play( note{ note::G5s, note::dH } );
	p.play( note{ note::B5, note::dH } );
	p.play( note{ note::C5, note::dQH } );
	p.play( note{ note::P, note::dQH } );
	p.play( note{ note::E5, note::dH } );
	p.play( note{ note::E5, note::dH } );
	p.play( note{ note::D5s, note::dH } );
	p.play( note{ note::E5, note::dH } );
	p.play( note{ note::D5s, note::dH } );
	p.play( note{ note::E5, note::dH } );
	p.play( note{ note::B5, note::dH } );
	p.play( note{ note::D5, note::dH } );
	p.play( note{ note::C5, note::dH } );
	p.play( note{ note::A5, note::dQH } );
	p.play( note{ note::P, note::dQH } );
	p.play( note{ note::C5, note::dH } );
	p.play( note{ note::E5, note::dH } );
	p.play( note{ note::A5, note::dH } );
	p.play( note{ note::B5, note::dQH } );
	p.play( note{ note::P, note::dQH } );
	p.play( note{ note::D5, note::dH } );
	p.play( note{ note::C5, note::dH } );
	p.play( note{ note::B5, note::dH } );
}
