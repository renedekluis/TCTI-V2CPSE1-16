#include "melody.hpp" 

void melody::play(note_player & p ){
	p.play( note{ note::G5, note::dH } );
	p.play( note{ note::P, note::dQ } );
	p.play( note{ note::G5, note::dQH } );
	p.play( note{ note::G5, note::dQH } );
	p.play( note{ note::D5s, note::dQH } );
	p.play( note{ note::C5, note::dQH } );
	p.play( note{ note::G5, note::dH } );
	p.play( note{ note::P, note::dQ } );
	p.play( note{ note::G5, note::dQH } );
	p.play( note{ note::G5, note::dQH } );
	p.play( note{ note::D5s, note::dQH } );
	p.play( note{ note::C5, note::dQH } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::D5s, note::dF } );
	p.play( note{ note::C5, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::D5, note::dF } );
	p.play( note{ note::P, note::dQ } );
	p.play( note{ note::G5, note::dH } );
	p.play( note{ note::P, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::A5s, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::D5s, note::dQ } );
	p.play( note{ note::D5s, note::dH } );
	p.play( note{ note::F5, note::dQ } );
	p.play( note{ note::D5s, note::dQ } );
	p.play( note{ note::D5s, note::dQ } );
	p.play( note{ note::D5, note::dQH } );
	p.play( note{ note::D5s, note::dQH } );
	p.play( note{ note::D5, note::dQH } );
	p.play( note{ note::C5, note::dQH } );
	p.play( note{ note::D5, note::dF } );
	p.play( note{ note::D5s, note::dQH } );
	p.play( note{ note::D5s, note::dF } );
	p.play( note{ note::G5s, note::dQH } );
	p.play( note{ note::G5s, note::dQH } );
	p.play( note{ note::G5s, note::dQH } );
	p.play( note{ note::G5s, note::dQ } );
	p.play( note{ note::G5s, note::dH } );
	p.play( note{ note::A5s, note::dQ } );
	p.play( note{ note::G5s, note::dQ } );
	p.play( note{ note::G5s, note::dQ } );
	p.play( note{ note::D5, note::dF } );
	p.play( note{ note::P, note::dQ } );
	p.play( note{ note::C5, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::D5s, note::dF } );
	p.play( note{ note::C5, note::dQ } );
	p.play( note{ note::G5, note::dQ } );
	p.play( note{ note::D5s, note::dF } );
	p.play( note{ note::C5, note::dQ } );
	p.play( note{ note::G5s, note::dH } );
	p.play( note{ note::A5s, note::dH } );
	p.play( note{ note::G5s, note::dQ } );
}
